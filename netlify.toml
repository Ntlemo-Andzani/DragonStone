$tomlContent = @'
[build]
  publish = "."
  command = "echo 'Static site deploy'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
'@

$tomlContent | Out-File -FilePath "netlify.toml" -Encoding UTF8